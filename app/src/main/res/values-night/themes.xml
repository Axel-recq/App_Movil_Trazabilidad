<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema para Modo Oscuro -->
    <style name="Theme.App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Configuración para modo oscuro incluida automáticamente -->
    </style>

    <!-- Para forzar el tema oscuro en ciertas actividades (opcional) -->
    <style name="Theme.App.Dark" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/colorPrimaryNight</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariantNight</item>
        <item name="colorOnPrimary">@color/colorOnPrimaryNight</item>

        <!-- Colores secundarios -->
        <item name="colorSecondary">@color/colorSecondaryNight</item>
        <item name="colorSecondaryVariant">@color/colorPrimaryVariantNight</item>
        <item name="colorOnSecondary">@color/colorOnSecondaryNight</item>

        <!-- Colores de superficies -->
        <item name="android:colorBackground">@color/colorBackgroundNight</item>
        <item name="colorSurface">@color/colorSurfaceNight</item>
        <item name="colorOnBackground">@color/colorOnBackgroundNight</item>
        <item name="colorOnSurface">@color/colorOnSurfaceNight</item>

        <!-- Estado y barras del sistema -->
        <item name="android:statusBarColor">@color/colorPrimaryDarkNight</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/colorPrimaryNight</item>

        <!-- Resto de configuraciones igual que el tema base -->
        <item name="elevationOverlayEnabled">true</item>
        <item name="materialCardViewStyle">@style/App.CardView</item>
        <item name="toolbarStyle">@style/App.Toolbar</item>
        <item name="textInputStyle">@style/App.TextInputLayout</item>
        <item name="buttonStyle">@style/App.Button</item>
        <item name="materialButtonOutlinedStyle">@style/App.Button.Outlined</item>
        <item name="materialButtonStyle">@style/App.Button</item>
    </style>

    <!-- Para forzar el tema claro en ciertas actividades (opcional) -->
    <style name="Theme.App.Light" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>

        <!-- Colores secundarios -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Colores de superficies -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <!-- Estado y barras del sistema -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>

        <!-- Resto de configuraciones igual que el tema base -->
        <item name="elevationOverlayEnabled">true</item>
        <item name="materialCardViewStyle">@style/App.CardView</item>
        <item name="toolbarStyle">@style/App.Toolbar</item>
        <item name="textInputStyle">@style/App.TextInputLayout</item>
        <item name="buttonStyle">@style/App.Button</item>
        <item name="materialButtonOutlinedStyle">@style/App.Button.Outlined</item>
        <item name="materialButtonStyle">@style/App.Button</item>
    </style>
</resources>